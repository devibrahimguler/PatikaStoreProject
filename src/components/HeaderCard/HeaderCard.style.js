import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin: 10
    },
    title: {
        fontSize: 25,
        fontWeight: "bold",
        color: "#75147C",
    },
    textInput: {
        backgroundColor:  "#ECEFF1",
        padding: 10,
        borderRadius: 10,
        marginTop: 10,
    },
});